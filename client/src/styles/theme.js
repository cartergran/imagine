import { css } from 'styled-components';

const recycle = {
  flexCenter: css`
    display: flex;
    justify-content: center;
    align-items: center;
  `
};

const theme = {
  recycle
};

export default theme;