import { css } from 'styled-components';

const variables = css`
  :root {
    // colors
    --disabled: #A0A0A0;

    // spacing
    --space-s: 8px;
    --space-m: 16px;
    --space-l: 24px;
  }
`;

export default variables;