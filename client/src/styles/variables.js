import { css } from 'styled-components';

const variables = css`
  :root {
    // colors
    --disabled: gray;

    // spacing
    --space-s: 8px;
    --space-m: 16px;
    --space-l: 24px;
  }
`;

export default variables;