import { css } from 'styled-components';

const variables = css`
  :root {
    // colors
    --disabled: gray;

    // spacing
    --space-s: 8px;
    --space-m: 16px;
    --space-l: 24px;
    --space-xl: 32px;

    // sizing
    --tile-size: 48px;
  }
`;

export default variables;