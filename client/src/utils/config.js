const title = 'imagine';
const day = '0';
const attempts = 5;
const duration = 805;

const boardLayout = {
  rows: 4,
  cols: 4
};

const msgs = {
  correct: 'Nice.',
  incorrect: 'Sorry.'
};

const config = {
  title,
  day,
  attempts,
  duration,
  boardLayout,
  msgs
};

export default config;