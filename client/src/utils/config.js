const title = 'imagine';
const day = '0';
const attempts = 5;
const duration = 805;

const board = {
  rows: 4,
  cols: 4
};

const msgs = {
  correct: 'Nice.',
  incorrect: 'Sorry.'
};

const config = {
  title,
  day,
  attempts,
  duration,
  board,
  msgs
};

export default config;